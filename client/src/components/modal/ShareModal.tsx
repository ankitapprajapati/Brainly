

interface shareModalProps{
    open : boolean,
    onClose : ()=> void,
}

const ShareModal = ({open,onClose}: shareModalProps) => {
  return (
    <div>
      
    </div>
  )
}

export default ShareModal
